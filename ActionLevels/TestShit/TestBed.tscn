[gd_scene load_steps=7 format=2]

[ext_resource path="res://ActionLevels/LevelCreator/DumbActors/DumbMisbeehave/DumbMisbeehave.tscn" type="PackedScene" id=1]
[ext_resource path="res://ActionLevels/TestShit/TestBed.gd" type="Script" id=3]

[sub_resource type="Animation" id=1]
length = 0.001

[sub_resource type="Animation" id=2]
resource_name = "test"
length = 3.0

[sub_resource type="Shader" id=3]
code = "shader_type canvas_item;
render_mode unshaded;
uniform vec4 color : hint_color = vec4(1.0);
uniform float global_alpha_color : hint_range(0.0, 1.0, 0.1) = 1.0;
uniform float width : hint_range(0, 10) = 1.0;
uniform int pattern : hint_range(0, 2) = 0; // diamond, circle, square
uniform bool inside = false;
uniform bool add_margins = true; // only useful when inside is false

void vertex() {
	if (add_margins) {
		VERTEX += (UV * 2.0 - 1.0) * width;
	}
}

bool hasContraryNeighbour(vec2 uv, vec2 texture_pixel_size, sampler2D texture) {
	for (float i = -ceil(width); i <= ceil(width); i++) {
		float x = abs(i) > width ? width * sign(i) : i;
		float offset;
		
		if (pattern == 0) {
			offset = width - abs(x);
		} else if (pattern == 1) {
			offset = floor(sqrt(pow(width + 0.5, 2) - x * x));
		} else if (pattern == 2) {
			offset = width;
		}
		
		for (float j = -ceil(offset); j <= ceil(offset); j++) {
			float y = abs(j) > offset ? offset * sign(j) : j;
			vec2 xy = uv + texture_pixel_size * vec2(x, y);
			
			if ((xy != clamp(xy, vec2(0.0), vec2(1.0)) || texture(texture, xy).a <= 0.0) == inside) {
				return true;
			}
		}
	}
	
	return false;
}

void fragment() {
	vec2 uv = UV;
	
	if (add_margins) {
		vec2 texture_pixel_size = vec2(1.0) / (vec2(1.0) / TEXTURE_PIXEL_SIZE + vec2(width * 2.0));
		
		uv = (uv - texture_pixel_size * width) * TEXTURE_PIXEL_SIZE / texture_pixel_size;
		
		if (uv != clamp(uv, vec2(0.0), vec2(1.0))) {
			COLOR.a = 0.0;
		} else {
			COLOR = texture(TEXTURE, uv);
		}
	} else {
		COLOR = texture(TEXTURE, uv);

	}
	
	if ((COLOR.a > 0.0) == inside && hasContraryNeighbour(uv, TEXTURE_PIXEL_SIZE, TEXTURE)) {
		COLOR.rgb = inside ? mix(COLOR.rgb, color.rgb, color.a) : color.rgb;
		COLOR.a += (1.0 - COLOR.a) * (color.a + global_alpha_color);
	} else if (COLOR.a > 0.0) {
		//COLOR.a = COLOR.a * global_alpha_color;
		COLOR = vec4(0.0, 0.0, 0.0, 0.0);
	}

}"

[sub_resource type="ShaderMaterial" id=4]
shader = SubResource( 3 )
shader_param/color = Color( 0, 0, 0, 1 )
shader_param/global_alpha_color = 1.0
shader_param/width = 5.0
shader_param/pattern = 0
shader_param/inside = false
shader_param/add_margins = true

[node name="TestBed" type="Node2D"]
script = ExtResource( 3 )

[node name="AnimationPlayer" type="AnimationPlayer" parent="."]
unique_name_in_owner = true
anims/RESET = SubResource( 1 )
anims/test = SubResource( 2 )

[node name="DumbMisbeehave" parent="." instance=ExtResource( 1 )]

[node name="AnimatedSprite" parent="DumbMisbeehave" index="0"]
material = SubResource( 4 )

[editable path="DumbMisbeehave"]
